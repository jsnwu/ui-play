test_case:
  id: search-and-open-link
  title: search and open a link
  established: true
  baseUrl: "{{ENV:BASE_URL}}"
  actions:
    - Navigate to {{baseUrl}}
    - Click Search with DuckDuckGo dropdown with locator '#searchbox_input'
    - Fill "Search with DuckDuckGo dropdown" with "jsnwu github" with locator '#searchbox_input'
    - Click Search button
    - Click jsnwu (Jason Wu) link
